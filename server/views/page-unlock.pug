extends master.pug

block body
  #root.is-fullscreen
    page-unlock(
      :page-id=pageId
      :page-title=JSON.stringify(pageTitle)
      :return-to=JSON.stringify(returnTo)
      :error=JSON.stringify(error || '')
    )
