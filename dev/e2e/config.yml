port: 3000
bindIP: 0.0.0.0
db:
  type: postgres
  host: '$(DB_HOST)'
  port: $(DB_PORT)
  user: '$(DB_USER)'
  pass: '$(DB_PASS)'
  db: $(DB_NAME)
  ssl: false
ssl:
  enabled: false
logLevel: info
logFormat: default
ha: false
agents:
  enabled: true
  provider:
    enabled: false
